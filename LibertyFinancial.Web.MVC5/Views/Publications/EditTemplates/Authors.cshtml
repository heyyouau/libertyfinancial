@model IEnumerable<Liberty.Data.Author>
@*<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <title>Remote file for Bootstrap Modal</title>
</head>
<body>*@
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Select Author</h4>
    </div>
    <div class="modal-body">
        <select id="authorSelector">
            @foreach (var e in Model)
            {
                <option value="@e.AuthorId" >@e.AuthorLastName , @e.AuthorFirstName</option>
            }
        </select>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary" onclick="setAuthor(this)">Add Author</button>
    </div>
@*</body>
</html>*@

<script type="text/javascript">
    function setAuthor(e) {
        e.preventDefault();
        $.ajax({
               url: 
        });
    }

</script>
